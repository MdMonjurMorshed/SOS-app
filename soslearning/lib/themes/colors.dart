import 'package:flutter/material.dart';

class AppColor {
  final white = const Color(0Xff9867f5);
  final elevatedButtonColor = const Color.fromRGBO(237, 132, 19, 1);
  final floatingButton = const Color.fromRGBO(237, 132, 19, 1);
  final SubIconColor1 = const Color.fromRGBO(237, 132, 19, 1);
  final SubIconColor2 = const Color.fromRGBO(100, 233, 120, 1);
  final SubIconColor3 = const Color.fromRGBO(250, 10, 90, 1);
  final SubModCircleAvater = const Color(0XFFffe6ff);
  final tabcolor1 = const Color(0xFFff33ff);
  final tabcolor2 = const Color(0xFF2aff2a);
  final tabcolor3 = const Color(0xFFffa31a);
  final backicon = const Color(0xFF000000);
  final medel_lengradient = LinearGradient(
      colors: [Color(0xFFffa31a), Color(0xFFffa3b1a)],
      begin: Alignment.centerLeft,
      end: Alignment.centerRight);
  final quiz_lengradient = LinearGradient(
      colors: [Color(0xFFd934eb), Color(0xFFa134eb)],
      begin: Alignment.centerLeft,
      end: Alignment.centerRight);
  final mock_lengradient = LinearGradient(
      colors: [Color(0xFF34c0eb), Color(0xFF346beb)],
      begin: Alignment.centerLeft,
      end: Alignment.centerRight);

  //ICON colors

  final share_but_right_arrow = Colors.white;
  final call_but_icon = Colors.white;

  AppColor();
}
