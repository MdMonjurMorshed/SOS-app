import 'package:flutter/material.dart';

class LearningAnalysis extends StatelessWidget {
  const LearningAnalysis({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Material(
      child: Center(
        child: Text('this is learning analysis page'),
      ),
    );
  }
}
