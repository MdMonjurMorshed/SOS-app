import 'package:flutter/material.dart';

class CourseSelection extends StatelessWidget {
  const CourseSelection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Material(
      child: Center(
        child: Text('you can select course here'),
      ),
    );
  }
}
